OPEN_LIVE_URL=http://localhost:3000

# NOTE: the studio no longer takes an OSC Personal Access Token (PAT). The PAT is
# held server-side by the open-live backend; the studio obtains a short-lived SAT
# at runtime from the backend's POST /api/v1/auth/token endpoint. See README /
# src/lib/sat.ts.
